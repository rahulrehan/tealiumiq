{#
/**
 * @file
 * Default theme implementation for a summary of a webform TealiumIQ handler.
 *
 * Available variables:
 * - settings: The current configuration for this handler.
 * - handler: The handler.
 *
 * @ingroup themeable
 */
#}
<dl>
  {% for key, setting in settings.tealiumiq_field_mapping %}
    <dt>{{ key }}</dt>
    <dd>{{ setting }}</dd>
  {% endfor %}
</dl>
